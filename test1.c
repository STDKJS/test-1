#include <stdio.h>
void main()
{
    printf("1. git init -> 처음에 무조건 해야함");
    printf("2. git add . -> (.)은 파일을 전부 올린다는 뜻(ex.git add index.html)도 가능");
    printf("3. git status -> 상태 알려줌");
    printf("4. git commit -m "first commit" -> commit은 히스토리 알려줌");
}
